#api key:
REVENIUM_METERING_API_KEY="hak_..."
REVENIUM_METERING_BASE_URL="https://api.revenium.ai"

#vendor API keys
ANTHROPIC_API_KEY="sk-ant-..."

# AWS Bedrock Configuration (for Bedrock support)
# AWS credentials - use one of these methods:
# Method 1: Direct credentials (not recommended for production)
# AWS_ACCESS_KEY_ID="AKIA..."
# AWS_SECRET_ACCESS_KEY="..."

# Method 2: AWS Profile (recommended)
# AWS_PROFILE="your-profile-name"

# Method 3: IAM Role (for EC2/Lambda/ECS)
# AWS_ROLE_ARN="arn:aws:iam::123456789012:role/your-role"

# Required for Bedrock
AWS_REGION="us-east-1"

# Optional Bedrock overrides
# BEDROCK_MODEL_ID="anthropic.claude-3-sonnet-20240229-v1:0"  # Override model ID
# REVENIUM_BEDROCK_DISABLE="1"  # Disable Bedrock support entirely

# Trace Visualization Fields (v0.3.0+)
# Optional fields for distributed tracing and observability
# REVENIUM_ENVIRONMENT="production"  # Deployment environment (production, staging, development)
# REVENIUM_REGION="us-east-1"  # Cloud region identifier (auto-detects from AWS_REGION, AZURE_REGION, GCP_REGION)
# REVENIUM_CREDENTIAL_ALIAS="prod-anthropic-key"  # Human-readable API key name for credential tracking
# REVENIUM_TRACE_TYPE="customer-support"  # Workflow category identifier (max 128 chars)
# REVENIUM_TRACE_NAME="Support Chat Session"  # Human-readable trace label (max 256 chars)
# REVENIUM_PARENT_TRANSACTION_ID="parent-txn-123"  # Parent transaction ID for distributed tracing
# REVENIUM_TRANSACTION_NAME="Generate Response"  # Human-friendly operation name

# Optional: Enable debug logging
# REVENIUM_LOG_LEVEL=DEBUG